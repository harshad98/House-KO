import React from "react";
import { Typography } from "@mui/material";

const Home = () => (
  <div>
    <Typography variant="h4" gutterBottom>
      Home Page
    </Typography>
    <Typography>
      Welcome to the Home Page! Navigate using the menu.
    </Typography>


  </div>
);

export default Home;
